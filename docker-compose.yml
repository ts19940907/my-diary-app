version: '3.8'

services:
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - AUTH_SECRET=${AUTH_SECRET:-local-dev-secret}
      - DATABASE_URL=file:/app/data/diary.db
    volumes:
      - ./frontend/data:/app/data
